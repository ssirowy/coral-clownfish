import BasicBot from './bots/base-bot';

// Reference your bot class here using an import statment like above.

// Add a new instance of your into here for it to be picked by the app.
export default const bots = [ new BasicBot() ];
